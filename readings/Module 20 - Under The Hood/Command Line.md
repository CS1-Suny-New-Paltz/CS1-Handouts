# Command Line

compile w/ javac
but what about dependencies?
\-cp vs buildpath

run with java
export runnable jar
run from command line without an IDE

use this to understand what the "no hotswapping" error means
outdated .class file bc of compile errors
clearing your IDE cached files
